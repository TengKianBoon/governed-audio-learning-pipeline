[project]
name = "governed-audio-learning-pipeline"
version = "0.1.0"
description = "Local-first pipeline turning spoken content into governed knowledge artifacts."
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
